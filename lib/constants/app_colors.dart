import 'package:flutter/material.dart';

class AppColors {
	static const background = Color(0xFF1E1E1E);    // 메인 배경 (짙은 회색)
	static const surface = Color(0xFF2A2A2A);       // 카드, 패널
	static const surfaceContainerHighest = Color(0xFF2A2A2A); // 프리뷰 패널 등

	static const onSurface = Color(0xFFE6E6E6);      // 주 텍스트
	static const textPrimary = Color(0xFFE6E6E6);    // 주 텍스트
	static const textSecondary = Color(0xFF999999);  // 부 텍스트

	static const primary = Color.fromARGB(255, 83, 19, 195);     // 하이라이트 (보라)
	static const secondary = Color(0xFF60D394);      // 초록 (성공)
	static const accent = Color.fromARGB(255, 98, 58, 167);      // 포인트 컬러 (보라 계열)
	static const warning = Color(0xFFFFC961);        // 주황
	static const error = Color(0xFFFF5555);          // 오류 (빨강)
	static const info = Color(0xFF64D1FF);           // 하늘
}
